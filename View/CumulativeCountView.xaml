<UserControl x:Class="RAP.View.CumulativeCountView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RAP.View"
      mc:Ignorable="d">

    <StackPanel Orientation="Vertical">
        <Grid>
            <!-- Title -->
            <Label Content="CumulativeCount" DockPanel.Dock="Left" Style="{StaticResource HeaderStyle}"/>
            <!-- Close window button -->
            <Button Height="30" Width="30" Margin="2,2,2,2" Click="Button_Click" Style="{StaticResource CloseWindow}">
                <StackPanel VerticalAlignment="Center" >
                    <TextBlock Text="X"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- The cumulative count table -->
        <ListView x:Name="CumulativeCountTable" BorderThickness="0"  
                  HorizontalContentAlignment="Center" Background="Beige">
            <ListView.View>
                <GridView >
                    <GridViewColumn Header="Year" DisplayMemberBinding="{Binding Item1}" Width="100"/>
                    <GridViewColumn Header="Publication(s)" DisplayMemberBinding="{Binding Item2}" Width="200"/>
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</UserControl>
